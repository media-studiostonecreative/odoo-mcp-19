# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
.venv_old/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.env.local

# MCP client configuration (may contain secrets)
.mcp.json

# Odoo JSON config (may contain credentials)
odoo_config.json

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db
wiki/

# Claude Code / tooling scratch
.claude/
.superpowers/
.omc/
docs/superpowers/

# uv lockfile (project uses pip + pyproject.toml)
uv.lock

# Unrelated scratch files
cyanview-*.html

# Flat copy of the package at the repo root (the real one lives in src/odoo_mcp/)
/odoo_mcp/

# Business Pulse dashboard (separate Next.js app — see dashboard/)
dashboard/node_modules/
dashboard/.next/
dashboard/out/
dashboard/data/
dashboard/*.tsbuildinfo
dashboard/coverage/
dashboard/next-env.d.ts
# The Python "lib/" rule above (for venvs) also matches any directory named
# "lib" anywhere in the repo, which silently swallows dashboard/src/lib/ —
# the dashboard's entire server-side library. Un-ignore it explicitly.
!dashboard/src/lib/
!dashboard/src/lib/**

# Local git worktrees used for isolated feature work (superpowers skills).
.worktrees/
