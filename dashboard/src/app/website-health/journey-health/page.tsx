import { ComingSoon } from "@/components/health/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Journey Health"
      note="Journey definitions (Education, Wholesale, Ecommerce) are already modeled in the database, seeded from real pages found on the live site — the live step-by-step measurement engine is next."
    />
  );
}
