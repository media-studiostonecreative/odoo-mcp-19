import { ComingSoon } from "@/components/health/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Forms"
      note="Quick Scan already verifies form presence on key pages (see Attention Center); a dedicated per-form health view with completion/drop-rate data needs analytics connected first."
    />
  );
}
