import { ComingSoon } from "@/components/health/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Technical"
      note="Availability, console-error, and network-failure checks already run in Quick Scan and feed the Attention Center — a dedicated technical-health drill-down view is next."
    />
  );
}
