import { ComingSoon } from "@/components/health/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Localization"
      note="The retail site has EN/FR/DE/IT/ES/PT/NL locale selectors (confirmed by the existing audit) — a per-page language coverage matrix is next."
    />
  );
}
