import { ComingSoon } from "@/components/health/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Performance"
      note="Lighthouse is now installed in this project — a scheduled per-page run with historical score trends is the next piece of work."
    />
  );
}
