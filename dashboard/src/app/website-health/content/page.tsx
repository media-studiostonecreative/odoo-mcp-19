import { ComingSoon } from "@/components/health/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      title="Content"
      note="Content findings from monthly audits already flow into the Attention Center and Audit History — a dedicated content-QA view (obsolete dates, pricing consistency, placeholder text) is next."
    />
  );
}
